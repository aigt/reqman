import { BSModalContext } from 'angular2-modal/plugins/bootstrap';
import { StructureEntity } from '../../structure-entity/structure-entity';

export class EditItemModelContext extends BSModalContext {
  public id: number;
}